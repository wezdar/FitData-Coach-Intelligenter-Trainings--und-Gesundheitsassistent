# Copy to .env for local development. Never commit real secrets.
POSTGRES_PASSWORD=change-me
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me-minio
FITDATA_JWT_SECRET=replace-with-at-least-32-random-characters
FITDATA_CORS_ORIGINS=["http://localhost:3000"]
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Synthetic local demo credentials (seed manually; no real user data)
DEMO_EMAIL=demo@fitdata-coach.de
DEMO_PASSWORD=FitData-Demo-2026!
