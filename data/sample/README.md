# Synthetic sample data

All files in this directory are generated for demonstration and testing. They
contain no real person or device identifiers. Timestamps use ISO 8601; mixed
distance units in `distance.csv` intentionally exercise the staging conversion
rules.

The canonical all-purpose ETL fixture is `activities.csv`. The remaining files
demonstrate the supported focused import schemas exposed in the interface.
